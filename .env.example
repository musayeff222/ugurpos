# Hostinger hPanel → Node.js Web App → Environment variables

NODE_ENV=production
PORT=3000

# Zorunlu: güçlü rastgele bir değer (ör. openssl rand -hex 32)
JWT_SECRET=degistirin-guclu-bir-anahtar

# Opsiyonel: SQLite ve yüklenen dosyalar (varsayılan: server/data)
# DATA_DIR=/home/u123456789/domains/siteniz.com/nodejs/data
